import User from '../models/userModel'
import express from 'express'

const usersRoute = express.Router()

usersRoute.get('/users', upload.single('image'), (req, res) => {
    const user = new User({


    })
})
